<Window x:Class="PL.Order.OrderListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Order"
        xmlns:pl="clr-namespace:PL"
        mc:Ignorable="d"
        Title="OrderListWindow" Height="640" Width="800"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        Loaded="Window_Loaded"
        Closed="Window_Closed">

    <Window.Resources>
        <pl:ConvertOrderStatusToBrush x:Key="OrderStatusToBrush"/>
        <pl:ConvertOrderTypeToBrush x:Key="OrderTypeToBrush"/>
    </Window.Resources>

    <!--External Grid-->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40*"/>
            <RowDefinition Height="500*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <!--Internal, Upper Grid-->
        <Grid HorizontalAlignment="Stretch" Height="auto" Grid.Row="0" VerticalAlignment="Stretch" Width="auto">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="Filter By:" HorizontalAlignment="Left" Margin="176,0,0,0" VerticalAlignment="Center"/>
            <ComboBox Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"      
                      ItemsSource="{Binding Source={StaticResource OrderStatusCollectionKey}}" 
                      SelectedItem="{Binding OrderStatus, Mode=TwoWay}" />
        </Grid>
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding OrderList}"
                  IsReadOnly ="True"
                  AutoGenerateColumns="False"
                  EnableRowVirtualization="True"
                  RowDetailsVisibilityMode="VisibleWhenSelected"
                  MouseDoubleClick="dgOrderList_MouseDoubleClick"
                  SelectedItem="{Binding SelectedOrder}">

            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding CourierId}" Header="Courier ID" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding OrderId}" Header="Order ID" Width="Auto"/>

                <DataGridTemplateColumn Header="Order Type" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Background="{Binding OrderType, Converter={StaticResource OrderTypeToBrush}}"
                    CornerRadius="14"
                    Padding="10,2"
                    Margin="5"
                    HorizontalAlignment="Center"
                    MinWidth="100">
                                <TextBlock Text="{Binding OrderType}"
                           FontWeight="Light"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Binding="{Binding AerialDistance}" Header="Aerial Distance" Width="Auto"/>

                <DataGridTemplateColumn Header="Order Status" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Background="{Binding OrderStatus, Converter={StaticResource OrderStatusToBrush}}"
                                    CornerRadius="14"
                                    Padding="10,2"
                                    Margin="5"
                                    HorizontalAlignment="Center"
                                    MinWidth="100">
                                <TextBlock Text="{Binding OrderStatus}"
                                           FontWeight="Light"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Binding="{Binding ScheduleStatus}" Header="Schedule Status" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding RemainingTime}" Header="Remaining Time" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding TotalDeliveryTime}" Header="Total DeliveryTime" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding TotalDeliveries}" Header="Total Deliveries" Width="Auto"/>
                <DataGridTemplateColumn Header="Actions" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Delete"
                                    Click="DeleteOrder_Click"
                                    Padding="6,2"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Button Content="Add" Grid.Row="2" HorizontalAlignment="Center" Height="50" Width="100" Click="BtnAdd_Click" />

    </Grid>
</Window>

