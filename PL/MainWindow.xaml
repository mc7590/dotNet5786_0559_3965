<Window x:Class="PL.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <!-- === Main Grid: 3 main sections === -->
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- System Clock Area -->
            <RowDefinition Height="*"/>
            <!-- Configuration Area -->
            <RowDefinition Height="Auto"/>
            <!-- Database Control Area -->
        </Grid.RowDefinitions>

        <!--       System Clock Area     -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- System clock display -->
            <Border Grid.Column="1"
                    Background="#00BFFF"
                    Padding="10"
                    CornerRadius="4"
                    HorizontalAlignment="Center">
                <Label FontSize="26"
                       FontWeight="Bold"
                       Foreground="White"
                       Content="00.00.0000 00:00"/>
            </Border>

            <!-- Time adjustment buttons -->
            <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,10,0,0">
                <Button Content="Add One Minute" Margin="3"/>
                <Button Content="Add One Hour" Margin="3"/>
                <Button Content="Add One Day" Margin="3"/>
                <Button Content="Add One Month" Margin="3"/>
                <Button Content="Add One Year" Margin="3"/>
            </StackPanel>
        </Grid>

        <!--     Configuration Area      -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <!-- Input Area -->
                <ColumnDefinition Width="*"/>
                <!-- Navigation Buttons -->
            </Grid.ColumnDefinitions>
        </Grid>

        <!--     Database Control Area   -->
        <Grid Grid.Row="2" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="65*"/>
                <ColumnDefinition Width="65*"/>
                <ColumnDefinition Width="65*"/>
                <ColumnDefinition Width="18*"/>
                <ColumnDefinition Width="47*"/>
            </Grid.ColumnDefinitions>

            <!-- Database management buttons -->
            <Button Content="Reset Database" Grid.Column="0" Margin="10,65,10,65" Height="40"/>
            <Button Content="Initialize Database" Grid.Column="1" Margin="10,65,10,65" Height="40"/>
            <Button Content="Handle Couriers" Grid.Column="2" Margin="10,65,10,65" Height="40"/>
            <Button Content="Handle Orders" Grid.Column="3" Margin="10,65,10,65" Height="40" Grid.ColumnSpan="2"/>
        </Grid>

    </Grid>
</Window>
