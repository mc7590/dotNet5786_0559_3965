namespace DalApi;
using DO;
public interface IDelivery: ICrud<Delivery>
{
    //void Create(Delivery item); //Creates new entity object in DAL //Stage 1
    //Delivery? Read(int id); //Reads entity object by its ID //Stage 1
    //List<Delivery> ReadAll(); //stage 1 only, Reads all entity objects //Stage 1
    //void Update(Delivery item); //Updates entity object //Stage 1
    //void Delete(int id); //Deletes an object by its Id //Stage 1
    //void DeleteAll(); //Delete all entity objects //Stage 1
}
