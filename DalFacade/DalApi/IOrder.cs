namespace DalApi;
using DO;
public interface IOrder: ICrud<Order>
{
    //void Create(Order item); //Creates new entity object in DAL //Stage 1
    //Order? Read(int id); //Reads entity object by its ID //Stage 1
    //List<Order> ReadAll(); //stage 1 only, Reads all entity objects //Stage 1
    //void Update(Order item); //Updates entity object //Stage 1
    //void Delete(int id); //Deletes an object by its Id //Stage 1
    //void DeleteAll(); //Delete all entity objects //Stage 1
}
